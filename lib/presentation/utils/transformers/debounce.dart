// import 'package:stream_transform/stream_transform.dart';

// Stream<T> Function(Stream<T> events, Stream<T> Function(T t) mapper)
//     debounceTransformer<T>(Duration duration) {
//   return (Stream<T> events, Stream<T> Function(T t) mapper) =>
//       events.debounce(duration).switchMap(mapper);
// }
